<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="styles.css">
    <title>Mijn Website</title>
    <style>
        .cookie-popup {
            position: absolute;
            bottom: 10px;
            left: 30px;
            right: 30px;
            padding: 20px;
            background-color: rgba(0, 0, 0, 0.8);
            color: #fff;
            font-family: Arial, sans-serif;
            font-size: 14px;
            line-height: 1.4;
            border-radius: 8px;
            z-index: 9999;
            }

        .cookie-popup p {
            margin-bottom: 10px;
            }

        .cookie-popup a {
            color: #fff;
            text-decoration: underline;
            }

        .cookie-popup button {
            background-color: #fff;
            color: #333;
            border: none;
            padding: 8px 16px;
            font-family: Arial, sans-serif;
            font-size: 14px;
            border-radius: 4px;
            cursor: pointer;
            }

            .circle img {
                max-width: 100%;
                max-height: 100%;
            }

        .square-container {
            display: flex;
            justify-content: center;
            position: absolute;
            top: 350px;
            left: 20px;
            width: 300px;
            margin-bottom: 30px; /* Ruimte onder de vierkanten */
        }


         body {
            margin: 0;
            padding: 0;
            background-image: url('https://i.imgur.com/ss1L9Hg.jpeg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            filter: brightness(80%);
}

        .search-bar {
            display: flex;
            justify-content: flex-end;
            margin-top: 10px;
        }

            .search-bar input[type="text"] {
                padding: 5px;
                width: 800px;
                height: 50px;
                background-color: black;
                color: white;
                border: none;
            }

            .search-bar input[type="submit"] {
                padding: 5px 10px;
                background-color: #ccc;
                border: none;
                cursor: pointer;
            }

        body {
            background-color: #3333FF;
            position: relative;
        }

        .image-container {
            position: absolute;
            top: 70px;
            left: 50px;
        }

            .image-container img {
                width: 250px;
                height: 250px;
                border-radius: 50%;
            }
            header {
                background: #FFA500;
            }
}


        .header {
            position: fixed;
            top: 0;
            width: 100%;
            height: 60px;
            background-color: white;
            color: black;
            padding: 10px;
            z-index: 999;
        }

        .content {
            margin-top: 60px; /* Voeg een topmarge toe om ruimte te maken voor de header */
        }
    </style>
</head>
<body>
   

    <div class="content">
        <!-- Vierkant voor menu -->
        <div class="square-container">
            <div class="square"></div>
        </div>

        <!-- Zoekbalk -->
        <div class="search-bar">
            <input type="text" placeholder="Zoeken...">
            <input type="submit" value="Zoek">
        </div>

      

        <!-- Overige inhoud van je website -->
</body>

<body>
  <div class="cookie-popup">
    <p>Deze website maakt gebruik van cookies om de gebruikservaring te verbeteren. Door deze site te blijven gebruiken, ga je akkoord met ons <a href="/privacy">privacybeleid</a>.</p>
    <button onclick="acceptCookie()">Accepteren</button>
  </div>

  <script>
    function acceptCookie() {
      var popup = document.querySelector('.cookie-popup');
      popup.style.display = 'none';

      var expirationDate = new Date();
      expirationDate.setDate(expirationDate.getDate() + 365);
      document.cookie = 'cookie_accepted=true; expires=' + expirationDate.toUTCString() + '; path=/';
    }

    var cookieAccepted = document.cookie.replace(/(?:(?:^|.*;\s*)cookie_accepted\s*\=\s*([^;]*).*$)|^.*$/, "$1");
    if (!cookieAccepted) {
      var popup = document.querySelector('.cookie-popup');
      popup.style.display = 'block';
    }
  </script>
</body>

</html>
